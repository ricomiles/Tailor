# The engines floor in package.json is Node >= 22.18 for a reason the code
# cannot recover from gracefully: the unit suite is loaded by Node type
# stripping, which the .mts imports depend on. Below that floor `pnpm test`
# fails with an opaque syntax error instead of an install-time refusal.
engine-strict=true
