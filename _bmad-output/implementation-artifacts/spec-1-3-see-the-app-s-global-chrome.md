---
title: 'Story 1.3 — See the app''s global chrome'
type: 'feature'
created: '2026-08-27'
status: 'done'
baseline_commit: '4a09ce84ed2e5f80d6476e53efa83b6d9fdef87e'
review_loop_iteration: 2
context: ['_bmad-output/implementation-artifacts/epic-1-context.md']
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** Every screen from here on renders inside global chrome that does not exist. Its 39px height is load-bearing — Epic 2 pins its action bar at `top: 39px` — and its pipeline counts are the first consumer of a cross-unit shape, the discipline this epic exists to establish.

**Approach:** Add a sticky, exactly-39px top bar to the root layout, styled by a CSS Module so `app/globals.css` stays diffable against its ported source. Declare the counts shape once in `core/` as a named zod schema with an inferred type, and have the layout supply a zero value today — Epic 2 swaps the supply, not the bar.

## Boundaries & Constraints

**Always:**
- The bar lives in `app/layout.tsx`, above `{children}`, so every screen carries it.
- Its border-box is exactly 39px including the 2px bottom rule. Epic 2 pins its action bar at `top: 39px`.
- Every color resolves through a `var(--*)` token. No hex literal.
  *(Amended 2026-08-27, review round 2: the original clause said "color, space, and
  font value". The design specifies a 39px bar, 20/14px gaps, an 18px divider and
  16/11/10px type, none of which sit on the `--space-*` scale (4/8/12/16/24/32).
  Those ship as literals, documented in `top-bar.module.css`'s header — the clause
  is corrected to what the story actually holds to rather than left failed.)*
- The counts shape is declared once, in `core/`, as a named zod schema with its inferred type, importing nothing but `zod` (AD-1).
- Copy comes verbatim from the design source.

**Ask First:**
- Reading counts or the board count from any real store. The DB and `boards.json` arrive in Story 1.5 and Epic 2.
- Any edit to `app/globals.css` — Story 1.2 froze it as a diffable port of its source.
  *(Amended 2026-08-27, review round 2, with the human's authorization. The count is
  **eight** deltas, not six: Story 1.2's review round 2 adopted two focus corrections
  with explicit sign-off, and those hunks are present in this working tree. They
  landed under 1.2's authority, not 1.3's — this clause is reconciled to that fact
  rather than claiming the file is untouched.)*

**Never:** the prototype's `jump` select or the divider preceding it; its outer shell (`min-height:100vh`, the app ground, the 14px base override); `.nav`/`.nav-brand`; app styles in `app/globals.css`; a client component, hook, or store; `app/page.tsx`.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Zero state — what ships today | counts all `0`, `boardCount` `0` | `0 discovered  0 tailored  0 approved  0 submitted`; right side reads `no boards yet` | N/A |
| Boards watched | `boardCount` `14` | right side reads `watching 14 boards` | N/A |
| One board | `boardCount` `1` | right side reads `watching 1 board` | N/A |
| Invalid counts | a negative or fractional count | schema parse fails naming the offending field | throw at the boundary; never render a coerced zero |

</frozen-after-approval>

## Code Map

- `app/layout.tsx` -- 32 lines. `<body>{children}</body>` at L29. **Do not touch L10-14 or L22-25** — the latter explains why props are typed `{ children: ReactNode }` rather than Next's generated `LayoutProps<"/">`: the generated types do not exist on a clean checkout and `pnpm typecheck` must pass.
- `app/globals.css` -- **read-only for this story.** Story 1.2 froze it as a diffable port; it now carries **eight** marked deltas after 1.2's review round 2, printing 12 `diff` hunks (8 value/declaration changes, 4 comment-only port notes). Supplies every token this story needs, plus global `box-sizing: border-box` and `body{margin:0;line-height:1.55}`.
- `components/top-bar/` -- new, beside `components/resume-document/`. Only `core/` is boundary-restricted.
- `components/top-bar/labels.ts` -- new. The validation boundary: values become text here and nowhere else, so this is where AC #5's throw lives. Split out of `top-bar.tsx` because that module imports a CSS Module, which Node's test runner cannot parse.
- `core/pipeline/` -- new file lands here; the four count names are pipeline states.
- `core/boards/board-count.ts` -- new (review round 1). `boardCountSchema`, the board count's single declaration, on the same discipline as the pipeline counts.
- `tests/` -- Node `--test` unit suite. Core's tests live outside `core/` because AD-1 forbids `node:test` inside it.
- `e2e/`, `playwright.config.ts` -- new (review round 1). The only place the 39px border-box, the sticky pin, and the bar's document position are actually measured. Runs against `next start` on a production build, not `next dev`.
- `scripts/run-tests.mjs` -- new (review round 1); extended in round 2. Collects `tests/**/*.test.mts` recursively, fails on an empty collection or a missing `tests/`, and fails on a test file it would never execute (wrong suffix, or outside `tests/`).
- `scripts/e2e-gate.mjs`, `scripts/verify.mjs` -- new (review round 2). The freshness marker for the Playwright suite and the `pnpm verify` orchestrator. Together they make a stale e2e run a `pnpm build` failure; see the round-2 change-log entry.
- `scripts/verify-boundaries.mjs` -- **edited** (rounds 1 and 2): the required `build` chain now includes `pnpm test`; round 2 added assertions on the *body* of every script the chain names (a name-only check let `"test": "true"` through) and the e2e freshness gate. Otherwise read-only.
- `package.json`, `tsconfig.json`, `.npmrc`, `.gitignore`, `README.md` -- **edited** (rounds 1 and 2). `package.json`: `"type": "module"`, the `>=22.18` engines floor, `test` / `test:e2e` / `verify`, and `@playwright/test`. `tsconfig.json`: `allowImportingTsExtensions`, `erasableSyntaxOnly`, `verbatimModuleSyntax`. `.npmrc`: `engine-strict=true`, so the Node floor refuses at install rather than failing opaquely at test time. `.gitignore`: Playwright artifacts and the freshness marker. `README.md`: the script table, the layout tree, the Node-floor rationale, and the one-time browser install.
- `eslint.config.mjs` -- read-only. AD-1: under `core/`, React, `next/*`, the ORM, Node built-ins, the `@/` alias, relative escapes, and deferred loading are all errors. `zod` is permitted.
- **Read-only, govern visuals and copy:** `_bmad-output/inputs/design_handoff_resume_tailoring/Tailor.dc.html` L23-45 (bar markup, with every size, tracking, and color token inline) and L951 (`boardsLabel`); `.../README.md` L82-85, L119-120.

## Tasks & Acceptance

**Execution:**
- [x] `core/pipeline/pipeline-counts.ts` -- declare `pipelineCountsSchema` over four non-negative integers (`discovered`, `tailored`, `approved`, `submitted`); export the inferred `PipelineCounts` type and a `ZERO_PIPELINE_COUNTS` constant derived through the schema. -- Epic 2 produces this same shape from posting statuses; declaring it here is the epic's highest-value invariant.
- [x] `components/top-bar/top-bar.module.css` -- the bar's styles: sticky at `top: 0`, `height: 39px`, `line-height: 1`, a 2px bottom rule, token-only values. -- `app/globals.css` is frozen, so app-layer styles need their own file.
- [x] `components/top-bar/top-bar.tsx` -- server component taking `{ counts: PipelineCounts; boardCount: number }`; renders the brand, the vertical divider, the four counts, and the right-aligned boards label. -- One component, no client runtime.
- [x] `app/layout.tsx` -- render `<TopBar counts={ZERO_PIPELINE_COUNTS} boardCount={0} />` inside `<body>`, above `{children}`. -- This call site is the seam Epic 2 replaces.

**Acceptance Criteria:**
- Given any screen, when it renders, then the bar is the first element the layout puts in `<body>` — ahead of `{children}` — and stays pinned to the viewport top while the page scrolls beneath it. (Measured among rendered elements: React reserves the literal first slot in `<body>` for its own hidden preamble container, so the original "first element in `<body>`" was unmeetable as written.)
- Given the rendered bar, when I measure its border-box, then it is exactly 39px tall, the 2px bottom rule included.
- Given the bar, when I inspect the brand, then it reads `tailor` in `var(--font-heading)` at weight 800 and 16px, followed by a 1px × 18px vertical divider.
- Given the repository, when I search for another declaration of those four count fields, then only `core/pipeline/pipeline-counts.ts` declares them.
- Given a negative, fractional, non-numeric, or missing count — or a board count that is negative, fractional, `NaN`, or `Infinity` — when the bar renders it, then the render throws at the label boundary and no coerced value reaches the page.


### Review Findings

*Code review 2026-08-27 (round 2) — four layers: blind-hunter, edge-case-hunter, verification-gap, acceptance-auditor. Raw layer output in `review-1-blind-hunter.md` … `review-4-acceptance-auditor.md`. The acceptance layer executed every Verification command (all exit 0) and mutation-tested AC #5; the verification-gap layer demonstrated each gap against a served production build rather than arguing it from the diff. 4 decision-needed, 23 patch, 5 deferred, 5 dismissed as noise. All four decisions were taken on 2026-08-28 and every patch applied; two decisions became patches, two became deferrals, bringing the totals to 26 applied and 6 deferred. Every claim below was verified against the code before triage, and the fixes that add or repair a test were mutation-checked.*

- [x] [Review][Decision] AC #5's throw is unenforced at the render path — the whole gate stays green with the validation boundary removed — **resolved 2026-08-28: deferred until Epic 2** — `app/layout.tsx` supplies `ZERO_PIPELINE_COUNTS` unconditionally, so no call site can exercise the throw until Epic 2 swaps the supply; revisit as part of that swap. Filed in `deferred-work.md`. Original finding: AC #5 requires that *the render* throws at the label boundary. Mutation-tested by the acceptance layer: replacing `countLabels`/`boardsLabel` in `top-bar.tsx` with direct interpolation leaves `pnpm build` at exit 0 (lint, typecheck, 20 unit tests, boundaries) and `pnpm test:e2e` at 9/9. The unit tests prove the two helpers throw; nothing proves the bar calls them, and `app/layout.tsx:41` only ever passes `ZERO_PIPELINE_COUNTS`, so no call site can expose the gap. Fix requires a choice: (a) a dev/test-only fixture route rendering `TopBar` with invalid counts, asserted by e2e — but the spec's Never list scopes routes out of this story; (b) a unit test that imports `top-bar.tsx` behind a CSS-Module stub loader; (c) accept the gap and record it as deferred until Epic 2 supplies real counts. [`components/top-bar/top-bar.tsx:30`, `components/top-bar/labels.ts:24`, `app/layout.tsx:41`]
- [x] [Review][Decision] `pnpm test:e2e` runs in no chain that anything executes — every rendered-output guarantee this story added is human-only — **resolved 2026-08-28: freshness-marker gate.** `scripts/e2e-gate.mjs` records a hash of the sources the suite observes; `verify:boundaries` — which `build` runs — fails when they have changed since the last recorded run, and `pnpm verify` (`scripts/verify.mjs`) turns the gate off for its own build step so it can be satisfied at all. Mutation-checked: changing `height: 39px` makes the next `pnpm build` fail naming the stale hash. Original finding: `build` deliberately excludes e2e, there is no `.github/`, no workflow, and no git hook; `verify:boundaries` asserts only that the *string* `pnpm build && pnpm test:e2e` exists in `package.json`, and is itself invoked only from `build`. So a green `pnpm build` proves `verify` is well-formed and never that it ran. Demonstrated: change `.bar { height: 39px }` to `44px` and `pnpm build` exits 0 — Epic 2's action bar, pinned at `top: 39px`, ships against a 44px bar. This is the repo's own stated anti-pattern (`spec-1-1:147`: "without this the fixtures ran only when a human typed the command"), which is why `verify:boundaries` was chained into `build`; the e2e link was not given the same treatment. Fix requires a choice: (a) chain `test:e2e` into `build` and accept the browser dependency and runtime cost; (b) have `verify:boundaries` assert a marker that `test:e2e` last ran against the current `.next/BUILD_ID`; (c) accept `pnpm verify` as a human-typed gate and say so in the spec. [`package.json:16`, `package.json:20`, `scripts/verify-boundaries.mjs:504-518`]
- [x] [Review][Decision] Story 1.3's frozen block declares `app/globals.css` read-only and "untouched here", while this diff edits it — **resolved 2026-08-28: frozen block amended** with the human's authorization. Boundaries/Ask First and the Code Map now record eight deltas and that Story 1.2's round-2 corrections landed in this working tree under 1.2's authority. The tokenization clause was reconciled in the same pass. Original finding: Boundaries/Ask First says *"Any edit to `app/globals.css`"* requires asking; Code Map says **read-only**; the Change Log says *"`app/globals.css` is untouched here."* The diff carries six hunks in that file: the header's delta count, two `[port note: …]` blocks, and the two focus-offset corrections. The work itself is legitimate — `spec-1-2` Iteration 3 records the human's explicit sign-off for corrections 6 and 7 — but it landed under Story 1.2's authority, and 1.3's frozen block was never reconciled to it. The frozen block carries `do not modify unless human renegotiates`, so the reconciliation is yours to authorize: (a) amend 1.3's frozen Boundaries and Code Map to record that 1.2's round-2 corrections landed in this working tree; (b) split the `globals.css` hunks out of 1.3's diff and land them under 1.2. [`_bmad-output/implementation-artifacts/spec-1-3-see-the-app-s-global-chrome.md:30`, `:48`, `app/globals.css:1`]
- [x] [Review][Decision] Correction 7's focus ring is clipped by `.seg { overflow: hidden }`, yet the defect it fixes is marked closed — **resolved 2026-08-28: deferred entry reopened, CSS unchanged** — `.seg-opt` has no consumer in the repo, so the clipped ring breaks nothing today; fix it when the first segmented control ships. Original finding: `.seg` sets `overflow: hidden`, which clips descendant ink overflow including a descendant's outline. Correction 7 moved `.seg-opt:has(input:focus-visible)` from `outline-offset: -2px` to `+2px`, which paints the ring entirely outside the `.seg-opt` border box — i.e. outside `.seg`'s padding box on the group's outer edges. The old `-2px` was at least drawn inside the clip. So the correction plausibly trades "invisible because accent-on-accent" for "invisible because clipped", while `deferred-work.md` strikes the entry through as **Closed 2026-08-27** and `spec-1-2` Iteration 3 records it resolved. Note there is **no consumer of `.seg-opt` in the repo today** (grep matches only `globals.css`), so nothing is broken at runtime — what is wrong is the closure claim. Fix requires a choice: (a) add `.seg { overflow: visible }` (or `overflow: clip; overflow-clip-margin`) as a ninth delta to a port whose delta count is already contested; (b) revert correction 7 to `-2px` and switch the ring to `var(--color-bg)` when checked; (c) reopen the deferred entry and leave the CSS as-is until a consumer exists. [`app/globals.css:202`, `app/globals.css:217`, `_bmad-output/implementation-artifacts/deferred-work.md:60-61`]

- [x] [Review][Patch] The "weights are actually loaded, not just declared" test reads the `@font-face` table, not the loaded faces — `Array.from(document.fonts)` yields a CSS-connected `FontFace` per `@font-face` rule regardless of whether the file was fetched, and the assertion maps to `f.weight` without touching `f.status`. Demonstrated: with every `*.woff2` request aborted, every glyph rendered in the metric-adjusted fallback while the weights array was unchanged and all nine tests passed. Weight 600 is `unloaded` on all three subsets even in the healthy build. Needs `await document.fonts.ready` plus `f.status === "loaded"`, or `document.fonts.check('800 16px Archivo')` [`e2e/top-bar.spec.ts:113-127`]
- [x] [Review][Patch] AC #3's 1px × 18px divider is measured by nothing — `grep -rn "divider" e2e/ tests/` returns nothing; the brand test reads only font properties and the geometry tests measure the `header` box. Demonstrated: deleting `height: 18px` collapses the empty element to zero and removes the divider from the bar, and **all nine tests still pass**. Needs a `boundingBox()` assertion of `{ width: 1, height: 18 }` [`components/top-bar/top-bar.module.css:44-49`, `e2e/top-bar.spec.ts`]
- [x] [Review][Patch] The colour-token test pins two of the bar's five token references — `--color-neutral-600` and `--space-4` sit in single-property declarations with no fallout. Demonstrated by injecting `:root { --color-neutral-600: initial }`: the boards label moved `rgb(125,121,121)` → `rgb(32,30,29)` and stopped reading as muted secondary, while the three values the suite reads were byte-identical and all nine tests passed. Same for `--space-4` — padding drops to 0, the brand goes flush to the edge, and `box.x === 0` / `box.width === clientWidth` stay true because they measure the border box [`e2e/top-bar.spec.ts:128-146`, `components/top-bar/top-bar.module.css:27`, `:72`]
- [x] [Review][Patch] `reuseExistingServer: !process.env.CI` is always true — no CI exists, so any stale listener on port 3100 is reused and `pnpm verify` builds a fresh bundle then measures the old server, which is precisely the failure mode the config's own comment argues against [`playwright.config.ts:28`]
- [x] [Review][Patch] `forbidOnly: !!process.env.CI` is always false — a committed `test.only` narrows the suite to one test and exits 0, silently skipping the sticky pin, colour tokens, font faces, body reset and zero-state copy. The unit suite's equivalent hole is closed by `run-tests.mjs`; the e2e suite's is not [`playwright.config.ts:18`]
- [x] [Review][Patch] `EXPECTED_BUILD_CHAIN` pins the script *name* `pnpm test` but never `scripts.test`'s body — `"test": "true"` satisfies the regex while the entire unit suite disappears, the exact substitution the chain guard exists to prevent. Same for `test:e2e` under `EXPECTED_VERIFY_CHAIN`. Assert the script bodies too [`scripts/verify-boundaries.mjs:487-518`]
- [x] [Review][Patch] `run-tests.mjs` collects only `tests/**/*.test.mts`, so a `tests/foo.test.ts` or a test outside `tests/` is silently never run while `build` stays green — the exact silent-skip the script's header says it exists to prevent. Its guard covers only the total-zero case, and `tsconfig.json`'s `include` covers `**/*.ts`, so such a file typechecks and lints cleanly with no other signal [`scripts/run-tests.mjs:17-24`]
- [x] [Review][Patch] The delta count is stale in three places and the parity command's stated expectation is unmeetable — `globals.css:1` now says eight deltas, 1.3 still says six in Boundaries, Code Map and Verification, and the command actually prints **12 hunks** (8 value/declaration changes + 4 comment-only port notes). As written, `expected: the same six deltas Story 1.2 left` can never be satisfied, so the check proves nothing about drift. The Change Log's own "9 diff hunks" restatement is also stale [`_bmad-output/implementation-artifacts/spec-1-3-see-the-app-s-global-chrome.md:48`, `:114`, `:117`]
- [x] [Review][Patch] The contrast deviation this story calls "escalated" never reached the escalation register — `deferred-work.md` has no Story 1.3 section and no entry for the 10px `--color-neutral-600` boards label at ~3.9:1. It lives only in a CSS comment and a spec paragraph, the two places a reader of the deferred register will not look. It is the one shipped-today WCAG failure with no tracked home [`components/top-bar/top-bar.module.css:9-12`, `_bmad-output/implementation-artifacts/deferred-work.md`]
- [x] [Review][Patch] `role="status"` is invented against the design source and makes the chrome a polite live region — `Tailor.dc.html:25` carries no role or label. `role="status"` is announced on change, and Epic 2 exists to make these counts change, so every update would announce all four labels on every screen. It is now hard-pinned by an e2e assertion. `role="group"` with the same `aria-label` matches static chrome (note a bare `aria-label` on a roleless `div` is ignored, so the role cannot simply be dropped) [`components/top-bar/top-bar.tsx:29`, `e2e/top-bar.spec.ts:81-82`]
- [x] [Review][Patch] The Code Map and Execution task list no longer describe what changed — `components/top-bar/labels.ts` is in neither, though it is where the AC #5 validation boundary actually lives; `verify-boundaries.mjs` gained a whole `EXPECTED_VERIFY_CHAIN` block beyond its "otherwise read-only" sanction; and `package.json`, `tsconfig.json`, `.gitignore` and `README.md` changed with no entry at all. Round 1 added four files to the Code Map, so the omission is inconsistent rather than a convention [`_bmad-output/implementation-artifacts/spec-1-3-see-the-app-s-global-chrome.md:45-57`]
- [x] [Review][Patch] `engines.node >=22.18` and `"type": "module"` land unexplained and unenforced — no `.npmrc` with `engine-strict`, no runtime guard, no change-log entry. The bump also silently replaces the epic-context rationale ("Node is pinned because better-sqlite3 declares a hard `node >= 22` floor") with Node's type-stripping floor, while the README still attributes it to better-sqlite3. On Node 22.0–22.17 `pnpm test` fails with an opaque syntax error instead of a legible engine error [`package.json:5-8`, `README.md`]
- [x] [Review][Patch] Nothing installs the Playwright browsers — no `postinstall`, no README step, no note in either spec. On a clean clone `pnpm verify` fails at the webServer step with a browser-not-found error, and the README setup block is unchanged. (Story 1.5 owns clean-machine setup, but the README line is owed now, since this story is what introduced the dependency) [`README.md`, `package.json:16`]
- [x] [Review][Patch] `latin-ext` was added in this diff and is unobserved by the font test added in the same diff — the assertion is `arrayContaining(["400","600","800"])` over `f.weight` with no subset dimension, so reverting to `subsets: ["latin"]` drops three of the nine faces and every test still passes. Collect `f.unicodeRange` in the same evaluate and assert both subsets are represented [`app/layout.tsx:20`, `e2e/top-bar.spec.ts:120-125`]
- [x] [Review][Patch] The document-order test's filter is tuned to one React/Next preamble form — it excludes only `hidden` and `SCRIPT`, so a framework-injected `<style>`, `<template>`, `<link>` or announcer child makes `rendered[0]` fail spuriously. Use an explicit skip-set [`e2e/top-bar.spec.ts:33-40`]
- [x] [Review][Patch] `BAR = "header:has-text('tailor')"` selects the bar by its copy — a copy change or a second header containing that substring breaks all nine tests at once with a strict-mode violation or "resolved to 0 elements", rather than failing the one assertion that owns the copy. `body > header` or `getByRole("banner")` is stable [`e2e/top-bar.spec.ts:11`]
- [x] [Review][Patch] `BoardCount` is exported and never referenced, so the single-declaration discipline holds only at runtime — `top-bar.tsx:23` and `labels.ts:35` both type the value as bare `number` while `counts` gets `PipelineCounts`. The spec contradicts itself here: its Task text still specifies `boardCount: number` and the code follows the Task, not the round-1 Change Log entry that claims the schema discipline [`core/boards/board-count.ts:13`, `components/top-bar/top-bar.tsx:23`, `components/top-bar/labels.ts:35`]
- [x] [Review][Patch] The Spec Change Log's counts are stale — "Six Playwright assertions now cover it" against a suite that ships **nine** tests, and the toolchain-flags entry reads as though `allowImportingTsExtensions` pre-existed when this diff adds all three flags in one hunk. ("removing the parse fails seven unit tests" does check out) [`_bmad-output/implementation-artifacts/spec-1-3-see-the-app-s-global-chrome.md:83`, `:85`]
- [x] [Review][Patch] `spec-1-2`'s frontmatter still reads `review_loop_iteration: 1` and `status: 'done'` while the same diff appends **Iteration 3** (2026-08-27) to its change log [`_bmad-output/implementation-artifacts/spec-1-2-port-the-modernist-design-system.md:5-7`]
- [x] [Review][Patch] The README `Layout` tree was not updated in a diff that rewrites the README's script table two lines above it — still reads `components/   # resume-document` though this story adds `components/top-bar/`, and `tests/`, `e2e/`, `playwright.config.ts` and the pre-existing `scripts/` are all absent [`README.md`]
- [x] [Review][Patch] `run-tests.mjs` throws a raw `ENOENT` in the exact scenario its comment claims to cover — `readdirSync(TEST_DIR, …)` is unguarded, so renaming or moving `tests/` produces a Node stack trace rather than the crafted "An empty suite is a failure, not a pass." message. (The build does still fail, so this is message quality, not a silent pass.) `result.error` and `result.signal` are likewise never reported, though `result.status ?? 1` exits correctly [`scripts/run-tests.mjs:20`, `:33-36`]
- [x] [Review][Patch] The `"every state the schema declares gets a label, and none other"` unit test is tautological — it asserts `countLabels(…).map(l => l.split(" ")[1])` equals `PIPELINE_STATES`, but `countLabels` is implemented as `PIPELINE_STATES.map(name => …${name}\`)`, so both assertions hold by construction and cannot fail. What it appears to intend — pinning the display copy and order against the design source — is untested [`tests/top-bar-labels.test.mts:38-44`]
- [x] [Review][Patch] The e2e colour test hard-codes palette values outside the token table — `rgb(243, 242, 242)` and `rgb(96, 93, 93)` duplicate `--color-bg` and `--color-neutral-700` in a second file, so a legitimate palette retune fails e2e with an opaque numeric mismatch. The story's own no-hardcoded-colour grep covers `app components` with `.ts/.tsx/.css` only, so it sees neither `e2e/` nor the `rgb()` form. Reading the token off `:root` and comparing tests the wiring without pinning the value [`e2e/top-bar.spec.ts:139`, `:145`]

- [x] [Review][Defer] `.bar { overflow: hidden }` silently truncates the counts and boards label, and the 380px e2e test enshrines a narrow viewport as covered while asserting only the height [`components/top-bar/top-bar.module.css:34`, `e2e/top-bar.spec.ts:101-105`] — deferred, by design (the module comment argues the clip explicitly, and the epic scopes the app to a wide desktop viewport)
- [x] [Review][Defer] `z-index: 40` is an unexplained literal in a file that carefully justifies its other literals [`components/top-bar/top-bar.module.css:17`] — deferred, pre-existing: the value matches the design source, and `deferred-work.md` already tracks the dialog-backdrop stacking conflict it creates
- [x] [Review][Defer] `playwright@1.62.1` sits in `dependencies` while `@playwright/test` is added to `devDependencies`, and the README's core-ban list names `playwright` explicitly [`package.json:26`, `package.json:33`] — deferred, pre-existing: added in `56ee9ca` (Story 1.1) and load-bearing for `tools/boundary-fixtures/core/canon/forbidden-package-playwright.ts`, so the fix is "move to devDependencies", not "remove"
- [x] [Review][Defer] `deferred-work.md` carries two separate `.dialog-backdrop` z-index entries rather than one amended entry, and the contrast entry forward-references a later section for its own missing inventory [`_bmad-output/implementation-artifacts/deferred-work.md`] — deferred, housekeeping
- [x] [Review][Defer] Correction 6 re-declares the identical `outline-offset: 2px` it already inherits from the base `:focus-visible` rule, so one of the eight counted deltas is a no-op; expressing it as a deletion of the source's override would be smaller and self-documenting [`app/globals.css:125`, `app/globals.css:179`] — deferred, pre-existing: a Story 1.2 port-discipline call, harmless as shipped
## Spec Change Log

**2026-08-27 — review round 1.** Multi-lens review (adversarial, edge-case, verification-gap) against baseline `4a09ce8`. Changes made to the spec and to the code it governs:

- **AC 1 restated.** "First element in `<body>`" is unmeetable under Next 16 — the built HTML opens `<body>` with React's hidden preamble container. Reworded to "first element the layout puts in `<body>`", and pinned by an e2e assertion over rendered elements.
- **New AC for the boundary throw.** The I/O matrix already required invalid counts to throw rather than render; nothing implemented it. `countLabels` and `boardsLabel` now parse, and the criterion is stated explicitly.
- **`boardCount` given a schema.** It was a bare `number` while `counts` had one. `core/boards/board-count.ts` declares it, on the same single-declaration discipline as the pipeline counts.
- **Tests joined the gate.** The suite existed but nothing ran it: `pnpm test` was in no chain, there is no CI, and a zero-match glob exited 0. It is now a required link in `build` (enforced by `scripts/verify-boundaries.mjs`), collects recursively, and fails on an empty collection.
- **`pnpm test:e2e` added.** The 39px contract Epic 2 depends on was human-verified only. Seven Playwright tests covered it at the end of this round (ten after round 2); both were mutation-checked (removing `height: 39px` fails two of them, removing the parse fails seven unit tests).
- **Toolchain flags.** `allowImportingTsExtensions`, `erasableSyntaxOnly` and `verbatimModuleSyntax`, added together: the first lets the `.mts` suite import `.ts` sources by their real extension, and without the other two that same source would pass `typecheck` and `build` while Node's type stripper could not load it at test time.
- **The hex-literal check was unrunnable.** As written it grepped `app/` including `globals.css`, the token table itself, so it matched 36 lines and could never report the "no matches" it expected. Scoped to exclude the token table.
- **Not changed, flagged for Story 1.2:** `app/globals.css` is untouched here, but it differs from its ported source by more hunks than 1.2's spec claims deltas. Either the count or the file needs reconciling. *(Resolved in round 2: 1.2 declares eight deltas, the file prints 12 hunks — 8 value or declaration changes plus 4 comment-only port notes — and 1.3's own text was reconciled to that.)*

**2026-08-27 — review round 2.** Multi-lens review (blind-hunter, edge-case, verification-gap, acceptance-auditor) against baseline `4a09ce8`, working tree. The acceptance layer executed every Verification command and mutation-tested AC #5; the verification-gap layer demonstrated each gap against a served production build. 4 decisions, 26 patches, 6 deferrals, 5 dismissed. Changes made:

- **The e2e suite now gates the build.** It ran only when a human typed `pnpm verify` — no CI, no hook, and `verify:boundaries` asserted the *script's text*, never that it ran. `scripts/e2e-gate.mjs` records a hash of the sources the suite observes; `verify:boundaries`, which `build` does run, fails when they have changed since. `pnpm verify` (now `scripts/verify.mjs`) turns the gate off for its own build step, since otherwise the build that must precede the e2e run would be blocked by the e2e run not yet having happened. Mutation-checked: changing `height: 39px` makes the next `pnpm build` fail naming the stale hash.
- **Script bodies are pinned, not just their names.** `EXPECTED_BUILD_CHAIN` matched `pnpm test` in the chain and never what `test` resolved to, so `"test": "true"` satisfied it while the whole unit suite vanished. Mutation-checked.
- **Three rendered contracts had no observer.** AC #3's 1x18 divider was measured by nothing (deleting `height: 18px` collapsed it with all nine tests green); the colour test read 2 of the bar's 4 token references, missing `--color-neutral-600` and `--space-4`; and the font test enumerated the `@font-face` table, which is populated whether or not a file was ever fetched. All three now fail on their regressions, mutation-checked.
- **`latin-ext` is pinned by preloads, not faces.** The reviewer's proposed check did not work: `next/font` emits an `@font-face` for every subset regardless of the `subsets` array and lets `unicode-range` choose, so `document.fonts` is byte-identical with and without `latin-ext`. What `subsets` controls is which subset files are preloaded — one `<link rel=preload>` each. Verified by dumping both builds.
- **`role="status"` → `role="group"`.** The design source carries no role; `status` is a polite live region, so once Epic 2 makes the counts change every update would announce all four labels on every screen.
- **Test collection hardened.** `run-tests.mjs` now fails on a missing `tests/`, on a test file it would never execute (wrong suffix or outside `tests/`), and distinguishes a spawn failure or signal from an assertion failure.
- **Toolchain floor enforced.** `.npmrc` sets `engine-strict=true`, so Node below the `>=22.18` type-stripping floor refuses at install rather than failing opaquely at test time.
- **The frozen block was reconciled**, with the human's authorization: the six-delta claim, and the "color, space, and font value" tokenization clause the shipped metrics never satisfied.
- **Deferred:** AC #5's render-path throw (no reachable call site until Epic 2 swaps the supply), and correction 7's clipped focus ring (reopened in `deferred-work.md` rather than fixed — `.seg-opt` has no consumer).

## Design Notes

**Why an explicit `height: 39px`.** 39px is not cosmetic — Epic 2's action bar is specified sticky at `top: 39px` (UX-DR25), so a drifting height opens a gap or an overlap on every posting screen. The prototype's own `padding: 9px 16px` plus a 2px rule does not produce it: under the inherited `line-height: 1.55` the 16px brand alone is 24.8px tall, giving ~44.8px. Set `height: 39px` with `line-height: 1`; the global `box-sizing: border-box` keeps the 2px rule inside the 39px.

**Why not `.nav`.** It is a site-nav component — 12/16px padding, an 18px `.nav-brand` with `margin-right: auto`. The chrome takes 9/16px and a 16px brand and carries no links, so overriding nearly every declaration is worse than a dedicated module. Story 1.2's "don't invent parallel component classes" rule guards the design system's vocabulary; this is app chrome, not a new `.btn`.

**Contrast deviation, escalated not patched.** The boards label ships at 10px `--color-neutral-600` (#7d7979) on `--color-bg` (#f3f2f2) — about 3.9:1, under the WCAG AA minimum of 4.5:1. Both the size and the token come from the design source, which this story gives authority over visuals, so the fix belongs there rather than in a local override. `--color-neutral-700` would clear it at about 5.8:1. Recorded in `top-bar.module.css` as well, so the next person to touch the file sees it. The 11px counts at `--color-neutral-700` already pass.

**Zero-state copy.** The design source sets the label to `no boards yet` when nothing is watched, `watching N boards` otherwise — so the bar this story ships reads `no boards yet`, not `watching 0 boards`. The epic's AC names only the populated form, and the epic gives the design source authority over copy. Singular `watching 1 board` has no source counterpart and is decided here.

```tsx
// app/layout.tsx — the call site Epic 2 replaces, and the only thing it replaces
<body>
  <TopBar counts={ZERO_PIPELINE_COUNTS} boardCount={0} />
  {children}
</body>
```

## Verification

**Commands:**
- `pnpm lint` -- expected exit 0.
- `pnpm typecheck` -- expected exit 0.
- `pnpm verify:boundaries` -- expected exit 0; confirms AD-1 still fires with a new `core/` file present.
- `pnpm test` -- expected exit 0; the unit suite, now a link in the `build` chain. An empty collection exits 1 rather than passing vacuously.
- `pnpm test:e2e` -- expected exit 0; ten assertions measuring the 39px border-box, the sticky pin, the bar's document position, the divider's 1x18 box, the loaded font faces and preloaded subsets, all four colour and space tokens, and the body reset, against a production build. Requires `pnpm build` first (or use `pnpm verify`, which chains both) and a browser from `pnpm exec playwright install chromium`.
- `pnpm build` -- expected exit 0. Fails deliberately when the e2e suite has not run against the current sources; `pnpm verify` is what clears it.
- `pnpm verify` -- expected exit 0. The full gate: build with the freshness gate off, then render.
- `grep -rn "discovered\|tailored\|approved\|submitted" core/ components/ app/ --include='*.ts' --include='*.tsx'` -- AC #4. Expected: the four names are *declared* only in `core/pipeline/pipeline-counts.ts`; every other hit is a use, not a second declaration.
- `SRC=_bmad-output/inputs/design_handoff_resume_tailoring/_ds/modernist-f8562a2f-380c-4e83-bd66-6cba1fb04c4a/styles.css; diff <(tail -n +3 "$SRC") <(tail -n +3 app/globals.css)` -- expected: **12 hunks**, covering the **eight** deltas Story 1.2 declares (8 value or declaration changes, plus 4 comment-only port notes), and nothing new. `diff` exits 1 whenever it prints, so read the hunks rather than gating on the code.
- `grep -rnE "#[0-9a-fA-F]{3,8}\b" app components --include='*.ts' --include='*.tsx' --include='*.css' | grep -v '^app/globals.css:'` -- expected: no matches. The `globals.css` exclusion is not a loosening: that file *is* the token table and necessarily holds every hex in the palette, so without it this command could never pass — it matched 36 lines as originally written. What it checks is that no app-layer code outside the token table hard-codes a color.
- `grep -rn "jump\|<select" app/ components/` -- expected: no matches.
- With `pnpm dev` running: `curl -sS localhost:3000` -- expected to contain `no boards yet` and `0 discovered`. Kill the server afterward.

**Manual checks (browser devtools, wide desktop viewport):**
- The bar's border-box measures exactly 39px, with the 2px rule inside it.
- Scrolling the page leaves the bar pinned at `top: 0` with content passing beneath it, no bleed-through.
- The brand's computed `font-weight` is 800 at 16px, resolving to Archivo.
- The counts are 11px uppercase and the boards label sits flush against the right edge.
- The divider's 1x18 box, the loaded faces, and the colour tokens are no longer manual — `pnpm test:e2e` measures them.
