# Review layer 4 — Acceptance Auditor

*Subagent with spec + epic context. Story 1.3, review round 2. Baseline `4a09ce8`, working tree.*

## Verification commands, executed

`pnpm lint` 0 · `pnpm typecheck` 0 · `pnpm verify:boundaries` 0 · `pnpm test` 0 (20 tests) · `pnpm build` 0 · `pnpm test:e2e` 0 (9 tests) · hex grep — no matches · `jump|<select` grep — no matches · built HTML contains `0 discovered` and `no boards yet`. **One command cannot produce its stated expectation** (finding 2).

## Findings

- **The diff edits `app/globals.css`, which the spec declares read-only and asserts is untouched.** Violates Boundaries *"Ask First: Any edit to `app/globals.css`"*, Code Map *"read-only"*, and the spec's own Change Log line *"Not changed, flagged for Story 1.2: `app/globals.css` is untouched here."* Evidence: six hunks — header `six marked deltas` -> `eight marked deltas`, two new `[port note: ...]` blocks, and two behaviour changes (`.input:focus-visible` offset `0` -> `2px`, `.seg-opt:has(input:focus-visible)` `-2px` -> `2px`). The 1.2 spec records human sign-off for corrections 6 and 7, so the escalation did happen — but under Story 1.2, and 1.3's frozen Boundaries block was never reconciled to it.
- **The globals.css parity verification command can no longer report what it claims.** Verification says *"expected: the same six deltas Story 1.2 left, and nothing new."* Ran it: **12 hunks** covering **eight** declared deltas. The Change Log's own restatement ("9 diff hunks") is also stale. As written the expectation is unmeetable, so it proves nothing about drift.
- **`top-bar.module.css` ships literal space and font values.** Violates the frozen Always clause *"Every color, space, and font value resolves through a `var(--*)` token."* Only `padding: 0 var(--space-4)` and the colours are tokenized; `gap: 20px`, `gap: 14px`, `height: 39px`, `height: 18px`, `width: 1px`, `font-size: 16px/11px/10px`, `letter-spacing: -0.02em/0.08em/0.1em`, `border-bottom: 2px` are literals. The file's header openly declares the deviation, but no Change Log entry records the renegotiation of a frozen constraint. The *"No hex literal"* half does hold.
- **AC 5 is not pinned at the render path — the entire gate stays green with the validation boundary removed.** AC 5: *"when the bar renders it, then the render throws at the label boundary."* **Mutation-tested:** replaced `countLabels`/`boardsLabel` in `top-bar.tsx` with direct interpolation, then ran `pnpm build` -> exit 0 (lint, typecheck, 20 unit tests, boundaries) and `pnpm test:e2e` -> 9/9 pass. Reverted. Nothing in the repo renders `TopBar` under test; the tests prove only that two helper functions throw, not that the bar calls them.
- **AC 4 has no verification command.** *"only `core/pipeline/pipeline-counts.ts` declares them"* appears nowhere in the Verification list. Manual check passes today, but the criterion is unmechanized, and the field names are restated as object literals in `tests/pipeline-counts.test.mts:17,27`, `tests/top-bar-labels.test.mts:21`, and as strings in `e2e/top-bar.spec.ts:89-94`.
- **AC 3's divider half is entirely unverified.** `e2e/top-bar.spec.ts:61-76` asserts the brand's weight, size and family; nothing anywhere asserts the divider's dimensions — not the e2e suite, not the Verification commands, not the Manual checks. `.divider`'s `width: 1px; height: 18px` can drift with every gate green.
- **`components/top-bar/labels.ts` exists in neither the Code Map nor the Execution task list.** The Tasks enumerate four files and place the render in `top-bar.tsx`; the implementation splits the validation boundary into an unlisted fifth module. `core/boards/board-count.ts`, `tests/`, `e2e/`, and `scripts/run-tests.mjs` were all added to the Code Map in round 1, so the omission is inconsistent rather than a blanket convention.
- **`scripts/verify-boundaries.mjs` changed beyond what the Code Map sanctions.** Code Map: *"the required `build` chain it asserts now includes `pnpm test`. Otherwise read-only."* The diff also adds a whole `EXPECTED_VERIFY_CHAIN` block (~18 lines) pinning a newly invented `verify` script. Defensible work, undeclared scope.
- **Manifest and toolchain files changed with no Code Map entry.** `package.json` gains `"type": "module"`, `engines.node` `>=22` -> `>=22.18`, a `verify` script and `@playwright/test`; `tsconfig.json` gains `allowImportingTsExtensions`, `erasableSyntaxOnly`, `verbatimModuleSyntax`; `.gitignore` and `README.md` are edited. The engines bump silently replaces the epic-context rationale (*"Node is pinned because better-sqlite3 declares a hard `node >= 22` floor"*) with the type-stripping floor, and nothing enforces it — no `.npmrc` with `engine-strict`, so on Node 22.0–22.17 `pnpm test` fails on the `.ts` imports rather than on a legible engine error.
- **`pnpm test:e2e` still runs in no chain that anything executes.** The Change Log's stated fix pattern was *"the suite existed but nothing ran it."* That was fixed for the unit suite; for e2e it was not. `verify:boundaries` only asserts the *string* `pnpm build && pnpm test:e2e` exists. AC 1, 2, and 3 are therefore proven only when a human types `pnpm verify`.
- **`BoardCount` is declared and never used**, so the "same single-declaration discipline" claim holds only for the runtime half. `core/boards/board-count.ts:13` exports it; `top-bar.tsx:23` and `labels.ts:35` both still type the value as bare `number` (grep confirms zero references). The spec contradicts itself: its Task text still specifies `boardCount: number`, and the code follows the Task, not the Change Log.
- **The contrast deviation is declared "escalated" but never reaches the escalation register.** `deferred-work.md` has no Story 1.3 section and no entry for the 10px `--color-neutral-600` boards label at ~3.9:1; the only two `top-bar` mentions sit inside Story 1.2 entries about something else. The deviation lives only in spec prose and a CSS comment — the two places a future reader of the deferred register will not look.
- **Undeclared behaviour in the component: clipping, stacking, and an invented ARIA live region.** (a) `.bar { overflow: hidden }` silently truncates the counts and boards label; the I/O & Edge-Case Matrix has no narrow-viewport row, yet `e2e/top-bar.spec.ts:101` enshrines a 380px viewport as covered while asserting only the height. (b) `role="status" aria-label="Pipeline counts"` (`top-bar.tsx:29`) is invented — the design source markup (`Tailor.dc.html:25`) carries no role or label — and `role="status"` makes static chrome a polite live region; it is now hard-pinned by an e2e assertion. (c) `z-index: 40` matches the source but is undeclared in the spec, and already produced a deferred entry noting the dialog backdrop will paint under the chrome.
- **Stale counts in the Spec Change Log.** *"Six Playwright assertions now cover it"* — the suite ships **nine** tests. (*"removing the parse fails seven unit tests"* does check out.) Separately, the toolchain-flags entry reads as though `allowImportingTsExtensions` pre-existed; the diff adds all three flags in one hunk.
- **README `Layout` block not updated in a diff that edits the README.** Still reads `components/   # resume-document` though this story adds `components/top-bar/`, and `tests/`, `e2e/`, and `scripts/` are absent from the tree listing while the script table two lines above was rewritten.
