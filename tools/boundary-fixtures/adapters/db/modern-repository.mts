// A stand-in adapter authored as .mts — an extension eslint-config-next's own
// resolver list does not cover, which is why eslint.config.mjs widens it.
export const modernRepository = { name: "fixture" };
