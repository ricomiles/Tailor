// A stand-in for an adapter sitting at the root of adapters/, not in a
// subfolder — the target-side case folder-mode element patterns never classify.
export const rootRepository = { name: "fixture" };
